#!/usr/bin/env sh
# Placeholder default entrypoint so this repo validates as an omnibenchmark
# module (see omnibenchmark.yaml). Real entrypoints land later; this is a no-op.
echo "boilerplate: no entrypoint implemented yet" >&2
